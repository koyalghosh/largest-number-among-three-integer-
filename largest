#include<stdio.h>			
#include<conio.h>
main()
{
	int a,b,c;
	a=printf("Enter your first number");
	scanf("%d",&a);
	b=printf("Enter your second number");
	scanf("%d",&b);
	c=printf("Enter your third number");
	scanf("%d",&c);
 	if (a==b && b==c && c==a)
	{
		printf("All the values are equal");
	}
	else if (a==b || b==c || c==a)
	{
		if (a>c && b>c)
		{
			printf("first and second number are greater than third number");
		}
		else if(c>a && c>b)
		{
			printf("third number is the greatest number");
		}
		else if (b>a && b>c)
		{
			printf("second number is the greatest number");
		}
		else if (a>b && c>b)
		{
			printf("first and third number are greater than second  number");
		}
		else if (a>b && a>c)
		{
			printf("first number is the greatest number");
		}
		else if (b>a && c>a)
		{
			printf("second and third number are greater than first number  number");
		}
		printf("   but some numbers  are equal change them to get the largest one");
	}
	else if (a>b)
	{
		if (a>c)
		{
			printf("first number is the greatest number");
		}
		else
		{
			printf("third number is the greatest number");
		}
	}
	else if (b>a)
	{
		if (b>c)
		{
			printf(" second number is the greatest");
		}
		else
		{
			printf("third number is the largest number");
		}
	}
getch();
}
